import { FormatoQuetzalPipe } from './formato-quetzal-pipe';

describe('FormatoQuetzalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoQuetzalPipe();
    expect(pipe).toBeTruthy();
  });
});
